# No secrets required — dummyjson.com is a public API with fixed demo credentials.
# Kept for parity with the other HealthSaaS repos and for future environments
# that may require auth.

ENVIRONMENT=staging
